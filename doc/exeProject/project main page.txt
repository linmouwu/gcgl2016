这个页面展示当前项目所有的过程，及其之间的状态。

所以主界面是一个过程的列表，以后可能会引入图示。

过程列表中的每一项是项目所选过程
包括过程名，状态，输入，颜色表示是否满足，输出，颜色标识是否产生，然后是执行按钮，
输入输出如果是产品，有相应属性来表示其是否已经满足。

过程有状态，不能开始（因为输入的状态未满足）
	可以开始（输入已经满足）
	完成（输出的状态已经满足）

有的时候执行按钮无法点击，因为过程已执行完或者前提条件不满足